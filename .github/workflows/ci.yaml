name: previewer

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Set up Go
        uses: actions/setup-go@v3
        with:
          go-version: 1.23.1

      - name: Check out code
        uses: actions/checkout@v3

      - name: lint
        run: make lint

      - name: build
        run: make build

      - name: test
        run: make test
